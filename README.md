# CS261-Data-Structure

The project involves using different data structure to test the difference in runtime.

The data structure involves linked list, array, queue, stack, tree, binary search tree & hash table.
